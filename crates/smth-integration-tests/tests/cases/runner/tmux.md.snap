---
source: crates/smth-integration-tests/tests/test.rs
expression: output
input_file: crates/smth-integration-tests/tests/cases/runner/tmux.md
---
# Runner tmux directive behavior

## Successful tmux command prints stdout

Successful tmux commands should append an exit annotation and then write stdout inside a fenced
code block.

    :tmux list-sessions -F '#S' (success)

```
0
```

## Tmux commands share runner socket state

Commands run via `:tmux` should operate on the same runner-managed tmux server/socket across
multiple invocations.

    :tmux new-session -d -s socket-check (success)
    :tmux list-sessions -F '#S' (success)

```
0
socket-check
```

    :tmux kill-session -t socket-check (success)
    :tmux has-session -t socket-check (failure)

```
can't find session: socket-check
```

---
vim: set ft=markdown:
