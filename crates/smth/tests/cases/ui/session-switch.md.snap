---
source: crates/smth/tests/test.rs
expression: output
input_file: crates/smth/tests/cases/ui/session-switch.md
---
# Session switch

This scenario launches `smth` in the runner's attached tmux pane and verifies
that pressing Enter on a live session switches that client to the selected
session.

    :bins jj cat tmux

> [!NOTE]
> 'jj', 'cat', and 'tmux' are available.

    :copy tests/fixtures/jjconfig.toml .jjconfig.toml (copied)

    :t rename-session -t 0 runner (success)
    :t new-session -d -s beta "printf 'beta target'; cat" (success)
    :t resize-window -t runner:0 -x 80 -y 10 (success)

Respawn the runner's current pane with `smth`. Shell directives receive the
runner's `TMUX` and `TMUX_PANE` environment, so this targets the pane currently
shown by the control-mode client.

    :$ tmux respawn-pane -k 'smth' (exit: 0)
    :settle -d 2s (settled)

Type a query that selects `beta`, wait for the picker to redraw, and accept it.
After accepting the selection, the control-mode client should switch to the selected session.

    :k bet
    :snap

```terminal
session: bet
  1/2 | [C-r] repo: none | [C-x] close, [C-f] flag
▌⬤ beta                                                                        ┃
────────────────────────────────────────────────────────────────────────────────






```

    :k enter
    :settle -d 2s (settled)

    :t display-message -p '#{client_session}' (success)

```
beta
```

---
vim: set ft=markdown:
