---
source: crates/smth/tests/test.rs
expression: output
input_file: crates/smth/tests/cases/ui/current-repo-header.md
---
# Current repo header

This scenario launches `smth` from inside a repository directory to verify
the picker header shows the repo inferred from the current working directory.

    :bins jj cat

> [!NOTE]
> 'jj', and 'cat' are available.

    :copy tests/fixtures/jjconfig.toml .jjconfig.toml (copied)

    :t rename-session -t 0 runner (success)
    :$ jj git init alpha (exit: 0)
    :$ jj describe -R alpha -m "alpha commit" (exit: 0)
    :t new-session -d -s alpha-live "cat" (success)
    :t new-session -d -s ui "cd alpha && smth" (success)
    :t resize-window -t ui:0 -x 80 -y 10 (success)
    :pane ui:0.0

This snapshot shows the initial picker state, including the current repo in the
header.

    :snap

```terminal
session:
  3/3 | [C-r] repo: ~/alpha, [C-o] onto: trunk() | [C-x] close, [C-f] flag
▌⬤ runner                                                                      ┃
────────────────────────────────────────────────────────────────────────────────






```

---
vim: set ft=markdown:
