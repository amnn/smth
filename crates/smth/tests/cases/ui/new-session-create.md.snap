---
source: crates/smth/tests/test.rs
expression: output
input_file: crates/smth/tests/cases/ui/new-session-create.md
---
# New session create

Selecting the ephemeral new-session row creates a session named by the query.
Without repo context, the new tmux session inherits the current working
directory and has no repo metadata.

    :bins jj tmux

> [!NOTE]
> 'jj', and 'tmux' are available.

    :copy tests/fixtures/jjconfig.toml .jjconfig.toml (copied)

    :t rename-session -t 0 runner (success)
    :t new-session -d -s ui "smth" (success)
    :t resize-window -t ui:0 -x 120 -y 10 (success)
    :pane ui:0.0
    :settle -d 2s (settled)

Type a unique session name and accept the new-session row.

    :k zeta
    :snap

```terminal
session: zeta
  0/2 | [C-r] repo: none
▌  zeta                                                                                                                ┃
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────






```

    :k Enter
    :settle -d 2s (settled)

The client should switch to the newly-created session, and no repo metadata
should be attached.

    :t display-message -p '#{client_session}' (success)

```
zeta
```

    :t list-sessions -F '#{session_name}:#{@smth.repo}' (success)

```
runner:
zeta:
```

---
vim: set ft=markdown:
